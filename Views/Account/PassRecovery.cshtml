@using Camping.App_GlobalResources
@model Camping.ViewModels.PassRecoveryViewModel
@{
    ViewBag.Title = @Resource.Recovery;
}

<div>
    <!-- banner-bottom -->
    <div class="banner-bottom">
        <!-- container -->
        <div class="container">
            <div class="faqs-top-grids">
                <div class="book-grids">
                    <div class="col-md-3 book-left">

                    </div>
                    <div class="col-md-6 book-left">
                        <div class="book-left-info">
                            <h2>@Resource.Recovery</h2>
                        </div>
                        <div class="book-left-form">
                            @using (Html.BeginForm("PassRecovery", "Account", null, FormMethod.Post, new {enctype = "multipart/form-data"}))
                            {
                                @Html.AntiForgeryToken()
                                @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @autocomplete = "off", @placeholder = Resource.Email} })
                                <p>@Html.ValidationMessageFor(model => model.Email, "", new {@class = ""})</p>

                                <input type="submit" id="login" value="@Resource.Send">
                            }
                        </div>
                    </div>
                    <div class="col-md-3 book-right">

                    </div>
                    <div class="clearfix"> </div>
                </div>
            </div>
        </div>
        <!-- //container -->
    </div>
    <!-- //banner-bottom -->
</div>

